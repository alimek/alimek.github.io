import { Flex } from "@chakra-ui/react";

export const Header = () => (
  <Flex
    id="header"
    flexDirection="row"
    px="m"
    py="m"
    borderBottomWidth={0.5}
    borderColor="borderLight"
    mb="xs"
  >
    Header
  </Flex>
);
