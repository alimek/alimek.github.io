import { ComponentStyleConfig } from "@chakra-ui/react";

export const Link: ComponentStyleConfig = {
  variants: {
    menu: {
      fontWeight: "bold",
      color: "primary",
    },
  },
  baseStyle: {
    cursor: "pointer",
  },
};
